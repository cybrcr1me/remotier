# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Signing and notarisation credentials for `bun run build:mac`. `.env.example` documents
# the names; the real file holds an app-specific password and never gets committed.
.env
.env.*
!.env.example

# Tauri
src-tauri/target
src-tauri/gen/schemas

# Rasterised from design/icon.svg; regenerate with `bunx tauri icon`
design/icon.png

# The updater signing key. Private half: never commit it, never rotate it - every
# installed copy only trusts the key it shipped with. See docs/RELEASING.md.
.tauri/
